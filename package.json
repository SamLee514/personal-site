{
  "name": "personal-site",
  "version": "1.0.0",
  "description": "personal website",
  "main": "app.js",
  "repository": "https://github.com/SamLee514/personal-site.git",
  "author": "Samuel Lee",
  "license": "MIT",
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^18.0.0",
    "sass": "^1.38.1",
    "ts-node": "^10.8.1",
    "typescript": "^4.4.2"
  },
  "scripts": {
    "build": "yarn generate && tsc && sass src/styles/style.scss:public/style.css && cp -r src/index.html src/assets src/content _redirects public && mv src/content_tree.json public",
    "generate": "cd src && node ../scripts/generateContentPaths.mjs"
  }
}
